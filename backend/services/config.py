RTSP_TRANSPORT = "tcp"   # try TCP first, fallback to UDP automatically
RTSP_URLS      = []      # populated per-request via /connect endpoint
VIDEO_FILE     = ""      # unused — RTSP-only mode
USE_VIDEO_FILE = False
